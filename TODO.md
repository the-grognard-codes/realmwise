Create an import method from .csv.

Implement a way of debugging API behavior such that we can see what calls are being made and the response to standard out.

Make it so the catalog is in a collapsed view when opened.

Fix the file upload on Android for images